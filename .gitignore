# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Design source files — heavy originals, kept on disk only.
# Processed, web-sized versions belong in src/assets/.
raw/

# Review copies of the sliced screens — lossless PNGs plus the contact sheet.
# The web-sized WebP versions in src/assets/projects/ are the ones that ship.
sliced/

# Figma exports waiting to be processed. Nothing here ships: the WebP versions
# written into src/assets/projects/ are what the site loads.
figma/
